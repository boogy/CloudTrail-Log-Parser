defaultPlatforms:
  - linux/arm64
  - linux/amd64

builds:
  - id: cloudtrail-log-parser
    dir: .
    main: ./cmd/main.go
    binary: bootstrap
    env:
      - CGO_ENABLED=0
      - GOOS=linux
    ldflags:
      - -extldflags "-static"
      - -s -w
      - -X main.version={{.Git.Tag}}
      - -X main.commit={{.Git.ShortCommit}}
      - -X main.date={{.Date}}
    flags:
      - -tags=lambda.norpc
      - -trimpath
    tags:
      - "{{.Git.Tag}}"
      - latest

labels:
  org.opencontainers.image.title: "AWS CloudTrail Log Parser"
  org.opencontainers.image.version: "{{.Git.Tag}}"
  org.opencontainers.image.revision: "{{.Git.ShortCommit}}"
  org.opencontainers.image.created: "{{.Date}}"
  org.opencontainers.image.description: "CloudTrail Log Parser - Parse AWS CloudTrail logs efficiently to reduce noise and cost."
  org.opencontainers.image.source: "https://github.com/boogy/CloudTrail-Log-Parser"
  org.opencontainers.image.licenses: "Apache License 2.0"
  io.buildpacks.build.metadata: '{"aws_lambda_function_handler": "bootstrap"}'
